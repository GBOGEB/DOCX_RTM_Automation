# Pipeline

- **scripts/extract_structure.py**: Extracts headings and `QQQ` tags from a DOCX file and outputs them as JSON.
- **pipeline/commands.sh**: Sets up the virtual environment, extracts structure, and converts DOCX to Markdown.

## Usage

```bash
bash pipeline/commands.sh
```
