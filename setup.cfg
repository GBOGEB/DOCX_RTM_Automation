[metadata]
name = rtm-automation
version = 1.0.0
description = Automated RTM processing for DOCX files

[options]
package_dir =
    = src
packages = find:
python_requires = >=3.8
install_requires =
    python-docx>=0.8.11

[options.packages.find]
where = src
